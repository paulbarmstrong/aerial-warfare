disableSerialization;

/*
_helipad = bluforHelipad;
if (side player == east) then {
	_helipad = opforHelipad;
};

if (player distance2D _helipad < 10 && isTouchingGround (vehicle player) && uiNamespace getVariable "repairState" == 2) then {
	[] spawn FNC_Sortie;
};

*/

true;